{% extends 'base.html.twig' %}

{% block title %}

    {{ parent() }} | Create a new serie

{% endblock %}

{% block body %}

    <div class="container">

        {#
            <h4 class="display-4 mt-3">Create Series</h4>
        #}

        {{ form_start(serieForm) }}
        {{ form_widget(serieForm) }}
            <button type="submit" class="btn btn-outline-dark mt-3">Send !</button>
        {{ form_end(serieForm) }}


    </div>

{% endblock %}

